  <table class="table">
    <tbody>
      <tr></tr>
      <tr>
      <th>ジャンル検索</th>
      </tr>
      <tr>
      <% @genres.each do |genre| %>
        <tr>
        <td><%= link_to genre.name, admin_genre_path(genre.id) %></td>
        </tr>
        <% end %>
        </tr>
    </tbody>
  </table>