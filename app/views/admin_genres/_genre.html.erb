
      <table class="table">
      <thead>
      <tr></tr>
      <tr>
      <th class ="text-center">ジャンル検索</th>
      </tr>
      </thead>

      <tbody>
      <% @genres.each do |genre| %>
        <tr>
          <% if genre.status == "有効" %>
        <td class="genre"><%= link_to genre.name, admin_genre_path(genre.id) %></td>
          <% end %>
        </tr>
        <% end %>
        </tr>
    </tbody>
  </table>