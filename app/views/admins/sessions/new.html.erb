<!DOCTYPE html>
<html>
<div class="container">
  <body>
       <center> <h2>管理者ログイン</h2>
    


      <img src="/assets/logo.png" class="img-circle"><br />
      <span style="width:100%;font-size:9pt;font-family:ふみゴシック;color:pink;filter:Blur(add=0, direction=315,strength=1) dropshadow(color=violet,offX=3,offY=2");>
／いい気分♪＼</span>
<br />
<br />



      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
      <%= f.label :email, "メールアドレス " %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

<br />
      <div class="field">
        <%= f.label :password, "パスワード " %><br />
        <%= f.password_field :password, autocomplete: "current-password" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
          </div>
      <% end %>

      <div class="actions">
        <%= f.submit "ログイン", class: "btn btn-primary" %>



      </div>
      <% end %>

<%= render "admins/shared/links" %>
      </center>
    </body>
    
